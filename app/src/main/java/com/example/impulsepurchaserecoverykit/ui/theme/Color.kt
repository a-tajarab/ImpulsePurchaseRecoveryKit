package com.example.impulsepurchaserecoverykit.ui.theme

import androidx.compose.ui.graphics.Color

// ── Primary — Soft Midnight Blue ──────────────────────────────────────────
// Deep, confident navy. Pure blue family — no purple or teal crossover.
val Teal900 = Color(0xFF0E162C)   // near-black midnight — deepest headers
val Teal700 = Color(0xFF1C2646)   // primary — buttons, icons, arcs
val Teal500 = Color(0xFF344870)   // mid-tone — progress fills, active states
val Teal200 = Color(0xFF6878A8)   // muted blue — borders, dividers
val Teal100 = Color(0xFFB0BAD4)   // pale blue — tinted containers
val Teal50  = Color(0xFFE0E3EC)   // barely-blue — surface variants, chips

// ── Accent — Warm Stone ───────────────────────────────────────────────────
// Muted, earthy warm stone — grounded and natural without any orange.
// Pairs with midnight blue the way raw linen pairs with dark denim.
val Terra700 = Color(0xFF6E5840)   // deep stone — dark text on light surfaces
val Terra500 = Color(0xFF9E8060)   // primary accent — FAB, scan button, chips
val Terra200 = Color(0xFFC8B098)   // mid stone — borders on stone surfaces
val Terra50  = Color(0xFFF2EDE8)   // whisper stone — stone-tinted backgrounds

// ── Semantic — Success ─────────────────────────────────────────────────────
val Success700 = Color(0xFF2E6E4A)
val Success100 = Color(0xFFD0EDDF)

// ── Semantic — Warning ─────────────────────────────────────────────────────
val Warning700 = Color(0xFF906800)
val Warning100 = Color(0xFFFFF0CC)

// ── Semantic — Error ──────────────────────────────────────────────────────
val Error700 = Color(0xFF962E2E)
val Error100 = Color(0xFFFCE8E8)

// ── Neutrals ──────────────────────────────────────────────────────────────
val Charcoal900  = Color(0xFF1A1E2A)   // primary text — cool dark
val Charcoal700  = Color(0xFF3A4058)   // secondary text, icons
val Charcoal400  = Color(0xFF8890A8)   // placeholder, disabled
val Charcoal200  = Color(0xFFC8CCD8)   // dividers, outlines
val IvoryBg      = Color(0xFFEBEBED)   // page background — clean cool grey
val IvorySurface = Color(0xFFFFFFFF)   // card surfaces — pure white lifts off grey